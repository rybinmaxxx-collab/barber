# Барбер от Бога — сайт барбершопа

Одностраничный сайт барбершопа «Барбер от Бога» (Москва, ул. Тарусская, 14к1).
Одиннадцать секций, тексты — из платформы бренда и карточки на Яндекс.Картах.

**Позиционирование:** «Считаем не филиалы, а годы». Платформа бренда — факты,
конкуренты Ясенево, дистанцирование, тон голоса — в [`BRAND.md`](BRAND.md).

## Стек

| Слой | Чем сделано |
|---|---|
| Каркас | Next.js 14 (App Router) + TypeScript |
| Стили | Классы макета в `app/globals.css` + Tailwind для утилит |
| Движение | CSS-переходы + IntersectionObserver (`components/Reveal.tsx`) |
| 3D | three.js: ножницы из примитивов, без внешних моделей |
| Хостинг | Vercel: сборка статическая (`output: 'export'`), параметры — в `vercel.json` |

Шрифты (Unbounded, Golos Text, JetBrains Mono — только кириллица и латиница, OFL)
**самохостятся** в `app/fonts` и подключаются из `app/fonts.css`: ноль сторонних
доменов, сайт не ломается, если Google Fonts недоступны.

Оформление: фон `#0e0f12`, текст `#eceef2`, акцент `#d9a066`, максимальная
ширина контента 1280 px, разделители — линии в 1 px.

## Структура

```
app/
  layout.tsx        мета, микроразметка, шрифты
  page.tsx          порядок блоков, реплика и футер
  globals.css       дизайн-система и стили всех блоков
  fonts.css         @font-face локальных шрифтов
  fonts/            woff2 (Unbounded, Golos Text, JetBrains Mono)
components/
  Stage.tsx         фон: 3D-ножницы на three.js, реакция на курсор и скролл
  Reveal.tsx        появление блоков и запуск первого экрана
  Header.tsx        фикс-шапка с якорями и кнопкой записи
  Hero.tsx          первый экран — заголовок по строкам
  Score.tsx         02 · Счёт
  Manifesto.tsx     03 · Позиция — «чего нет / что есть»
  Craft.tsx         04 · Ремесло — четыре шага
  Price.tsx         05 · Прайс
  Fathers.tsx       06 · Отцы и сыновья
  Works.tsx         07 · Работы — сетка 3×2
  Master.tsx        08 · Мастер и «второе кресло»
  Reviews.tsx       09 · Слова
  Faq.tsx           10 · Вопросы — аккордеон на <details>
  Location.tsx      11 · Как дойти — контакты и карта
lib/
  content.ts        все тексты сайта — правки текста делаются здесь
  schema.ts         микроразметка @graph
  asset.ts          пути к файлам из public с учётом подпути деплоя
```

## Деплой

Сайт живёт на Vercel: проект подключён к репозиторию, каждый push собирается
автоматически. Параметры сборки заданы в `vercel.json` (`npm run build`,
раздаётся папка `out`), чтобы не зависеть от настроек в панели Vercel — именно
их расхождение с `output: 'export'` раньше давало `404: NOT_FOUND` на корне.

Если понадобится GitHub Pages, нужен подпуть: собирать с
`NEXT_PUBLIC_BASE_PATH=/<имя-репозитория>` и включить Pages в
Settings → Pages → Source: GitHub Actions (из workflow это сделать нельзя).

## Команды

```bash
npm install
npm run dev        # localhost:3000
npm run build      # статическая сборка в out/
npm run typecheck
```

## Решения по реализации

1. **Ножницы собраны процедурно** из примитивов three.js, а не загружены как
   `.glb`: лицензии на чужие модели, вес файла и правило «ноль сторонних
   запросов». Если WebGL недоступен, на канвас вешается класс `.fallback` —
   остаётся градиент, вёрстка не меняется.
2. **Анимации без библиотек.** Появление блоков — CSS-переход по классу,
   который вешает IntersectionObserver; при `prefers-reduced-motion: reduce`
   всё показывается сразу, а сцена рисуется одним кадром.
3. **Работы — обычная сетка**, а не горизонтальная лента на pin: на телефоне
   лента конфликтует с вертикальным скроллом.
4. **Фотографий пока нет** — в `public/images` лежат SVG-заглушки под размеры
   макета. Заменить на реальные снимки: шесть работ, портрет Надира.

## Что заменить перед публикацией

- Фотографии в `public/images/` (сейчас заглушки).
- `og.jpg` для соцсетей — файла ещё нет, ссылка в мета убрана.
- Проверить, что виджет Яндекс.Карт в `lib/content.ts` ведёт на нужную карточку.
